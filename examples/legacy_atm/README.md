# banco-atm (sample legacy system)

A small GnuCOBOL ATM used as a fixture for Reversa: login with account + PIN,
balance, withdrawal (limit, multiples of 10), deposit, transfer with fee,
statement (last 10 movements). Persistence in indexed/sequential .DAT files.
No tests, no docs — on purpose.
