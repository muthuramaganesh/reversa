# Risk register

| Risk | Severity | Description | Mitigation | Related |
|---|---|---|---|---|
| R-1 | medium | No recognisable structure extracted from reversa/__init__.py; manual reading required. | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-001 |
| R-2 | medium | No recognisable structure extracted from reversa/__main__.py; manual reading required. | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-002 |
| R-3 | medium | No recognisable structure extracted from reversa/agents/__init__.py; manual reading required. | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-003 |
| R-4 | medium | No recognisable structure extracted from reversa/llm/__init__.py; manual reading required. | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-004 |
| R-5 | medium | No decision logic detected in reversa/__init__.py; business rules for __INIT__ may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-005 |
| R-6 | medium | No decision logic detected in reversa/agents/__init__.py; business rules for __INIT__ may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-006 |
| R-7 | medium | No decision logic detected in reversa/llm/__init__.py; business rules for __INIT__ may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-007 |
| R-8 | medium | No decision logic detected in reversa/llm/heuristic.py; business rules for HEURISTIC may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-008 |
| R-9 | medium | No decision logic detected in tests/conftest.py; business rules for CONFTEST may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-009 |
| R-10 | medium | No decision logic detected in tests/test_analysis.py; business rules for TEST_ANALYSIS may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-010 |
| R-11 | medium | No decision logic detected in tests/test_backend.py; business rules for TEST_BACKEND may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-011 |
| R-12 | medium | No decision logic detected in tests/test_confidence.py; business rules for TEST_CONFIDENCE may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-012 |
| R-13 | medium | No decision logic detected in tests/test_manifest.py; business rules for TEST_MANIFEST may live elsewhere (data, configuration, copybooks). | Resolve by documented owner decision before cutover; until then keep the behaviour behind a parity test marked pending. | GAP-013 |
| R-14 | medium | 34 inferred claims may encode wrong assumptions about legacy behaviour. | Run @needs-validation scenarios against the legacy system first; promote to confirmed only on green. |  |
