# Design — ORCHESTRATOR

## Structure
ORCHESTRATOR is organised into 10 classs: State, load, save, Orchestrator, __init__, run, status, _persist, _write_plan, _write_readme.

## Data
No persistent data recovered.

## Dependencies
ORCHESTRATOR depends on __FUTURE__ (import). ORCHESTRATOR depends on JSON (import). ORCHESTRATOR depends on TIME (import). ORCHESTRATOR depends on TRACEBACK (import). ORCHESTRATOR depends on DATACLASSES (import). ORCHESTRATOR depends on PATHLIB (import). ORCHESTRATOR depends on TYPING (import). ORCHESTRATOR depends on .AGENTS (import). ORCHESTRATOR depends on .AGENTS.BASE (import). ORCHESTRATOR depends on .CONFIDENCE (import). ORCHESTRATOR depends on .LLM (import). ORCHESTRATOR depends on .MODELS (import). ORCHESTRATOR depends on .PROJECT (import).

_Derived from claims: C-058, C-059, C-060, C-061, C-062, C-063, C-064, C-065, C-066, C-067, C-068, C-069, C-070, C-217_
