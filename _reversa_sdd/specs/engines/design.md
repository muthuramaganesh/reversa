# Design — ENGINES

## Structure
ENGINES is organised into 3 classs: Engine, detect_engines, resolve_engines.

## Data
No persistent data recovered.

## Dependencies
ENGINES depends on __FUTURE__ (import). ENGINES depends on DATACLASSES (import). ENGINES depends on PATHLIB (import).

_Derived from claims: C-032, C-033, C-034, C-212_
