# Design — PROJECT

## Structure
PROJECT is organised into 7 classs: Project, __init__, inventory, lines, text, numbered, code_files.

## Data
No persistent data recovered.

## Dependencies
PROJECT depends on __FUTURE__ (import). PROJECT depends on OS (import). PROJECT depends on PATHLIB (import). PROJECT depends on .MODELS (import).

_Derived from claims: C-071, C-072, C-073, C-074, C-218_
