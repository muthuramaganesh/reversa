# Design — SCOUT

## Structure
SCOUT is organised into 6 classs: Scout, user_prompt, heuristic, run, _unit_for, _write_inventory.

## Data
No persistent data recovered.

## Dependencies
SCOUT depends on __FUTURE__ (import). SCOUT depends on COLLECTIONS (import). SCOUT depends on TYPING (import). SCOUT depends on ..ANALYSIS (import). SCOUT depends on ..MODELS (import). SCOUT depends on .BASE (import).

_Derived from claims: C-122, C-123, C-124, C-125, C-126, C-127, C-226_
