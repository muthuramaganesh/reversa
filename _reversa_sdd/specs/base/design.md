# Design — BASE

## Structure
BASE is organised into 11 classs: Context, write, selected_units, Agent, system_prompt, user_prompt, heuristic, run, _evidence_list, add_claims, record_warnings. BASE is organised into 4 classs: Backend, generate, strip_json, get_backend.

## Data
No persistent data recovered.

## Dependencies
BASE depends on __FUTURE__ (import). BASE depends on ABC (import). BASE depends on DATACLASSES (import). BASE depends on PATHLIB (import). BASE depends on TYPING (import). BASE depends on ..LLM.BASE (import). BASE depends on ..MODELS (import). BASE depends on ..PROJECT (import). BASE depends on __FUTURE__ (import). BASE depends on JSON (import). BASE depends on RE (import). BASE depends on ABC (import). BASE depends on TYPING (import). BASE depends on ..AGENTS.BASE (import). BASE depends on .HEURISTIC (import). BASE depends on .ANTHROPIC_BACKEND (import). BASE depends on OS (import).

_Derived from claims: C-094, C-095, C-096, C-097, C-098, C-099, C-100, C-101, C-142, C-143, C-144, C-145, C-146, C-147, C-148, C-149, C-150, C-221, C-229_
