# Design — INSTALLER

## Structure
INSTALLER is organised into 10 functions: _skill_text, _entry_text, _write, install, w, status, update, _section_before, uninstall, add_engine.

## Data
No persistent data recovered.

## Dependencies
INSTALLER depends on __FUTURE__ (import). INSTALLER depends on JSON (import). INSTALLER depends on SHUTIL (import). INSTALLER depends on TIME (import). INSTALLER depends on PATHLIB (import). INSTALLER depends on . (import). INSTALLER depends on .AGENTS (import). INSTALLER depends on .ENGINES (import). INSTALLER depends on .MANIFEST (import). INSTALLER depends on .ORCHESTRATOR (import).

_Derived from claims: C-035, C-036, C-037, C-038, C-039, C-040, C-041, C-042, C-043, C-044, C-213_
