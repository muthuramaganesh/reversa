# Design — CLI

## Structure
CLI is organised into 16 functions: _root, _backend, cmd_install, cmd_update, cmd_status, cmd_uninstall, cmd_add_engine, cmd_run, cmd_migrate, cmd_answer, cmd_export, cmd_report ….

## Data
No persistent data recovered.

## Dependencies
CLI depends on __FUTURE__ (import). CLI depends on ARGPARSE (import). CLI depends on JSON (import). CLI depends on SYS (import). CLI depends on PATHLIB (import). CLI depends on . (import). CLI depends on .CONFIDENCE (import). CLI depends on .LLM (import). CLI depends on .MODELS (import). CLI depends on .ORCHESTRATOR (import).

_Derived from claims: C-017, C-018, C-019, C-020, C-021, C-022, C-023, C-024, C-025, C-026, C-210_
