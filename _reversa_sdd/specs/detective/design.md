# Design — DETECTIVE

## Structure
DETECTIVE is organised into 5 classs: Detective, user_prompt, heuristic, run, _write.

## Data
No persistent data recovered.

## Dependencies
DETECTIVE depends on __FUTURE__ (import). DETECTIVE depends on RE (import). DETECTIVE depends on TYPING (import). DETECTIVE depends on ..ANALYSIS (import). DETECTIVE depends on .BASE (import).

_Derived from claims: C-102, C-103, C-104, C-105, C-106, C-222_
