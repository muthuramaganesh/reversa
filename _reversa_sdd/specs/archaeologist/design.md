# Design — ARCHAEOLOGIST

## Structure
ARCHAEOLOGIST is organised into 5 classs: Archaeologist, user_prompt, heuristic, run, _write.

## Data
No persistent data recovered.

## Dependencies
ARCHAEOLOGIST depends on __FUTURE__ (import). ARCHAEOLOGIST depends on TYPING (import). ARCHAEOLOGIST depends on ..ANALYSIS (import). ARCHAEOLOGIST depends on ..MODELS (import). ARCHAEOLOGIST depends on .BASE (import).

_Derived from claims: C-083, C-084, C-085, C-086, C-087, C-219_
