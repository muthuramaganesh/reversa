# Design — REVIEWER

## Structure
REVIEWER is organised into 5 classs: Reviewer, user_prompt, heuristic, run, _write.

## Data
No persistent data recovered.

## Dependencies
REVIEWER depends on __FUTURE__ (import). REVIEWER depends on TYPING (import). REVIEWER depends on ..CONFIDENCE (import). REVIEWER depends on ..MODELS (import). REVIEWER depends on .BASE (import).

_Derived from claims: C-117, C-118, C-119, C-120, C-121, C-225_
