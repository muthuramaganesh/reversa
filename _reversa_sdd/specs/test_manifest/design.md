# Design — TEST_MANIFEST

## Structure
TEST_MANIFEST is organised into 4 functions: _quiet, test_install_status_update_uninstall_preserve, test_uninstall_purge_removes_modified, test_add_engine.

## Data
No persistent data recovered.

## Dependencies
TEST_MANIFEST depends on PATHLIB (import). TEST_MANIFEST depends on REVERSA (import). TEST_MANIFEST depends on REVERSA.MANIFEST (import).

_Derived from claims: C-168, C-169, C-170, C-235_
