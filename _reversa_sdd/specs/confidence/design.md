# Design — CONFIDENCE

## Structure
CONFIDENCE is organised into 9 classs: Distribution, total, index, add, distribution, distribution_by_unit, traceability_density, _norm, verify_evidence.

## Data
No persistent data recovered.

## Dependencies
CONFIDENCE depends on __FUTURE__ (import). CONFIDENCE depends on COLLECTIONS (import). CONFIDENCE depends on DATACLASSES (import). CONFIDENCE depends on PATHLIB (import). CONFIDENCE depends on .MODELS (import).

_Derived from claims: C-027, C-028, C-029, C-030, C-031, C-211_
