# Design — WRITER

## Structure
WRITER is organised into 5 classs: Writer, user_prompt, heuristic, run, _write_unit.

## Data
No persistent data recovered.

## Dependencies
WRITER depends on __FUTURE__ (import). WRITER depends on COLLECTIONS (import). WRITER depends on TYPING (import). WRITER depends on ..MODELS (import). WRITER depends on .BASE (import).

_Derived from claims: C-128, C-129, C-130, C-131, C-132, C-227_
