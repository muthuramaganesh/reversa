# Design — MIGRATION

## Structure
MIGRATION is organised into 5 functions: _slug, Migration, user_prompt, heuristic, run.

## Data
No persistent data recovered.

## Dependencies
MIGRATION depends on __FUTURE__ (import). MIGRATION depends on RE (import). MIGRATION depends on TYPING (import). MIGRATION depends on ..MODELS (import). MIGRATION depends on .BASE (import).

_Derived from claims: C-107, C-108, C-109, C-110, C-111, C-223_
