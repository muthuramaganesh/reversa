# Design — PROCESS

## Structure
PROCESS is organised into 15 functions: _plain, _cond_english, narrate, business_context, overview_text, ProcessAgent, user_prompt, heuristic, ev, para_facts, walk, run ….

## Data
No persistent data recovered.

## Dependencies
PROCESS depends on __FUTURE__ (import). PROCESS depends on RE (import). PROCESS depends on TYPING (import). PROCESS depends on ..ANALYSIS (import). PROCESS depends on .BASE (import).

_Derived from claims: C-112, C-113, C-114, C-115, C-116, C-224_
