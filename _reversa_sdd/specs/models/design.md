# Design — MODELS

## Structure
MODELS is organised into 27 classs: Confidence, weight, ClaimKind, Severity, Evidence, ref, Claim, to_dict, from_dict, Gap, to_dict, from_dict ….

## Data
No persistent data recovered.

## Dependencies
MODELS depends on __FUTURE__ (import). MODELS depends on JSON (import). MODELS depends on RE (import). MODELS depends on DATACLASSES (import). MODELS depends on ENUM (import). MODELS depends on PATHLIB (import). MODELS depends on TYPING (import).

_Derived from claims: C-051, C-052, C-053, C-054, C-055, C-056, C-057, C-216_
